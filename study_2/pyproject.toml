[project]
name = "study2"
version = "0.1.0"
description = "Study 2 — self-description under exit affordances (METHODOLOGY §9)"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
analysis = ["pandas>=2.2", "statsmodels>=0.14", "matplotlib>=3.8"]
